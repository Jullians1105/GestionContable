# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
node_modules
dist
dist-ssr
*.local
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Claude Code
.claude/
.claude.json
CLAUDE.md
# Docker
.env
backups/
# MCP config (contains API keys)
.mcp.json
# n8n workflows (pueden contener credenciales en texto plano, ej. servicePassword)
n8n/workflows/*.json
*.dump
*.tar.gz
env_backup*.txt
# Reportes y anexos DIAN en docs/: los .xlsx traen datos financieros reales de la empresa
# (NITs, proveedores, montos) y los anexos técnicos PDF pesan decenas de MB. Ninguno debe
# entrar al repo — una vez commiteados quedan en el historial para siempre.
# El catálogo con lo que importa de los anexos está en docs/dian-tipos-documento.md.
docs/*.xlsx
docs/*.pdf
