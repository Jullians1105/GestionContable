// SMMLV y auxilio de transporte por año — fuente única compartida con el frontend.
// Ver /shared/salaryConstants.json (así backend y frontend no se desincronizan cada año).
const SALARY_CONSTANTS = require('../../../shared/salaryConstants.json');

module.exports = { SALARY_CONSTANTS };
